# Verhindert unbeabsichtigte Ausführung von Installationsskripten fremder Pakete.
# Ausnahmen werden in package.json unter pnpm.onlyBuiltDependencies gepflegt.
strict-peer-dependencies=false
auto-install-peers=true
