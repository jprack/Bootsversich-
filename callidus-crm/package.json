{
  "name": "callidus-crm",
  "version": "0.1.0",
  "private": true,
  "description": "Lokales Boots-CRM für Callidus — Node/Express/SQLite + React/Vite",
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:lan": "cross-env CRM_HOST=0.0.0.0 npm run dev",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3"
  }
}
